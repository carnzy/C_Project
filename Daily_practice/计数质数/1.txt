#include<iostream>
using namespace std;
class Solution
{
    public:
    int countPrimes(int n)
    {
        vector<bool> primesMap(n,true);
        int count = 0;
        for(int i= 2;i<n;i++)
        {
            if(primesMap[i])
            {
                count++;
                for(int j = 2 * i;)
            }
        }
    }
}